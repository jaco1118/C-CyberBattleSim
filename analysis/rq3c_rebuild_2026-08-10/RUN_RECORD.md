# Run record — Task RQ3C-REBUILD v2

Date: 2026-08-11.

## Command

```
cd analysis/rq3c_rebuild_2026-08-10
/cs/student/project_msc/2025/sec/slchan/conda_envs/ccbs/bin/python compute_rq3c_rebuild.py
```

## Every input path read (all read-only, none modified)

- `cyberbattle/agents/cx_step2_registration/drift_{10-15,30-40,80-100}.csv` — `attenuation_ratio_full`, `delta_h_v_norm`, `event_phase`, `seed`, `scenario_id`, `episode` columns.
- `cyberbattle/agents/cx_step2_registration/eventgraph_{band}/*/event_graph.jsonl` — `pre_edges`, `changed_node_id`, `changed_node_degree`, `changed_node_discovered`, per event.
- `cyberbattle/agents/cx_step2_registration/eventgraph_{band}/*/event_episode.jsonl` — `final_root_owned_count`, `root_owned_departures`, `event_counts`, per episode (the "actual"/mechanical side).
- `cyberbattle/agents/cx_step2_static/eventgraph_{band}/*/event_episode.jsonl` — `final_root_owned_count`, per episode (the "baseline" side, averaged per (band,seed,scenario_id) cell).

## RNG / seed

- The regression fits themselves (STEP 1, Section A, STEP 2a/2b/2c point estimates) use no RNG at all — plain OLS over fully assembled data.
- STEP 2b's detectable-R² floor uses `scipy.stats.ncf`/`brentq` — deterministic root-finding, no RNG.
- STEP 3's bootstrap: `np.random.default_rng(20260810)`, 10,000 resamples, resampling unit = row of the 117-cell `cell_agg` dataframe (`rng.integers(0, n_cells, n_cells)`), matching `compute_rq1a_regression.py`'s own bootstrap pattern.

## Row counts in/out at every stage (ARTIFACT, reproduced from `run_output.log`)

| band | registration episodes | no static pair (dropped) | no structure events (kept, NaN structure) | assembled |
|---|---|---|---|---|
| 10-15 | 640 | 0 | 0 | 640 |
| 30-40 | 1,770 | 0 | 0 | 1,770 |
| 80-100 | 2,000 | 0 | 0 | 2,000 |
| **pooled** | **4,410** | **0** | **0** | **4,410** |

| band | event_graph.jsonl total events | discovered==1 (used for 2-hop/degree) | drift-CSV `immediate` rows (used for propagation) | exact-zero attenuation_ratio_full (treated as 0 contribution) |
|---|---|---|---|---|
| 10-15 | 20,837 | 9,236 | 9,127 | 14 |
| 30-40 | 78,734 | 32,426 | 31,964 | 330 |
| 80-100 | 93,078 | 40,225 | 39,527 | 130 |

Cell-level aggregation: 117 cells (`rq3c_cell_level_117.csv`), matching the population confirmed in STEP 0.

## Outputs (all committed alongside this record)

`compute_rq3c_rebuild.py` (script), `rq3c_rebuild_episodes.csv` (4,410 rows, the assembled analysis dataset), `rq3c_rebuild_diagnostics.csv`, `rq3c_step1_reproduction.csv`, `rq3c_sectionA_degree_nulls.csv`, `rq3c_sectionA_three_fits.csv`, `rq3c_step2b_degree_vs_floor.csv`, `rq3c_step2c_episode_vs_cell.csv`, `rq3c_cell_level_117.csv`, `rq3c_step3_bootstrap_cell_level.csv`, `run_output.log` (full console output of the run reported here), this `RUN_RECORD.md`.

## Wipe test

If this machine were wiped and only the repository survived: `cx_step2_registration/`, `cx_step2_static/` and the manifest/checkpoints they were built from are themselves NOT committed (bulk run data, per this project's standing convention) — so this specific dataset would need to be regenerated by re-running `compute_attenuation_analysis.py` under the same CX registration configuration (flags recorded in `cx_step2_registration/eventgraph_*/run_metadata_*.json`: `allow_undiscovered_removal=true`, `uncapped_join=true`, `patch_service_dynamic_enabled=true`, `allow_undiscovered_property=false`, checkpoint SHA-256 hashes recorded per seed) before this script could be re-run — it is not committed-from-scratch reproducible today. Every number in the reply this record accompanies traces to a committed script (`compute_rq3c_rebuild.py`) and committed CSV output; only the raw CX sweep itself sits outside git, consistent with how every other drift-CSV-based analysis in this project is scoped.
